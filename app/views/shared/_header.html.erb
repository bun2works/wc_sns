<nav class="navbar sticky-top navbar-expand-lg navbar-dark py-0 mb-5 nav--border">
  <div class="container">
    <%# ロゴ部分 %>
    <div class="nav-item">
      <a class="navbar-brand" href="/"><img src="/assets/wcLogo.png" alt="navber"></a>
    </div>
    <%# メニュー格納時のボタン %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <%# メニュー %>
    <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
      <% if team_signed_in? %>
        <%= link_to team_path(current_team), class: "nav-item mr-4 fa--light" do %>
          <i class="fas fa-users fa-lg"></i> <%= current_team.name %>
        <% end %>
      <% else %>
        <%= link_to new_team_session_path, class: "nav-item mr-4 fa--light" do %>
          <i class="fas fa-user-times fa-lg"></i> ログイン
        <% end %>
      <% end %>
      <form class="nav-item form-inline mb-2 mb-lg-0" action="/" accept-charset="UTF-8" method="get">
        <input type="text" name="q" id="q" value="" class="form-control" placeholder="検索" >
      </form>
    </div>
  </nav>
</div>